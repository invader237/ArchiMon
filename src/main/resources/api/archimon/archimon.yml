get:
  tags:
    - archimon
  summary: Get all ArchiMon
  operationId: getAll
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './ArchiMonDTO.yml'

